  const [modalIsOpen, setIsOpen] = useState(false);
  const [errors, setErrors] = useState({})




function openModal() {
  setIsOpen(true);
}
function closeModal() {
  setIsOpen(false);
}

<Modal
         isOpen={modalIsOpen} 
         closeModal={closeModal} 
         props={categories} 
         ariaHideApp={false} 
         createItem={createItem}
         errors={errors}
         openModal={openModal}
          />


<button className="modal__open"  onClick={openModal}>Add Item</button>